function odd_even(number) {
      if (number > 0) {
          alert('Число ' + number + ' положительное.');
      }  else if (number < 0) {
          alert('Число ' + number + ' отрицательное.');
      } else {
          alert('Вы загадали 0');
      }
}

odd_even(0);

